---
const { title, description, image } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const imagemAbsoluta = new URL(image || '/images/og-mundo-bebe.png', Astro.site).toString();
---
<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>%F0%9F%91%B6</text></svg>" />
    <title>{title} | Mundo Bebê</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />
    <meta name="google-site-verification" content="AFQmk0EicCrPLG25m9s-2JTl8IR5paLf9pNyeLl70Ek" />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Mundo Bebê" />
    <meta property="og:title" content={`${title} | Mundo Bebê`} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:locale" content="pt_BR" />
    <meta property="og:image" content={imagemAbsoluta} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={`${title} | Mundo Bebê`} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={imagemAbsoluta} />

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-SSF5JH3DWN"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-SSF5JH3DWN');
    </script>
  </head>
  <body>
    <header class="topo">
      <a href="/" class="marca">Mundo Bebê</a>
      <nav>
        <a href="/cadeirinha-carro/">Cadeirinha para carro</a>
        <a href="/carrinho-bebe/">Carrinho de bebê</a>
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer class="rodape">
      <p>
        <strong>Aviso de transparência.</strong> Este site participa de programas
        de afiliados e pode receber comissão pelas compras qualificadas feitas
        através dos links, sem custo adicional para você.
      </p>
      <p>Preços citados são faixas de referência e mudam com o tempo.</p>
      <p><a href="/privacidade/">Política de Privacidade</a></p>
    </footer>
    <style is:global>
      :root {
        --cor-texto: #262521;
        --cor-texto-suave: #6b6a63;
        --cor-fundo: #fbfaf6;
        --cor-superficie: #ffffff;
        --cor-borda: #e4e1d6;
        --cor-acento: #4f7d8c;
        --cor-vale-saber: #c98a3c;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: -apple-system, "Segoe UI", Arial, sans-serif;
        color: var(--cor-texto);
        background: var(--cor-fundo);
        line-height: 1.6;
      }
      h1, h2, h3 { font-family: Georgia, "Times New Roman", serif; font-weight: 600; }
      a { color: var(--cor-acento); }
      a:hover { text-decoration-thickness: 2px; }
      .topo {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem 1.5rem;
        border-bottom: 1px solid var(--cor-borda);
        background: var(--cor-superficie);
      }
      .marca { font-weight: 700; text-decoration: none; color: var(--cor-texto); font-family: Georgia, serif; }
      .topo nav a { color: var(--cor-texto); text-decoration: none; margin-left: 1.5rem; }
      main { max-width: 720px; margin: 0 auto; padding: 2rem 1.5rem; }
      .rodape {
        max-width: 720px;
        margin: 3rem auto 0;
        padding: 1.5rem;
        border-top: 1px solid var(--cor-borda);
        color: var(--cor-texto-suave);
        font-size: 0.85rem;
      }
    </style>
  </body>
</html>
